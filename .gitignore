*.iml
.gradle
/local.properties
.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
/app/build
/app/release
*.apk
*.aab

# Local JDK used for builds
.jdk/

# Kotlin compiler daemon state - machine-local, never meaningful to commit
.kotlin/

# Session-local planning artifacts from AI coding assistants
.artifacts/

# iOS: Xcode project is generated by `xcodegen generate` from ios/project.yml (see ios/README.md),
# never committed itself. Secrets.plist holds the real Supabase URL/anon key and is written by
# Codemagic from its own env vars at build time - only the .example template is committed.
/ios/VibeFitness.xcodeproj/
/ios/VibeFitness/Resources/Secrets.plist
/ios/Pods/
/ios/.build/
/ios/build/
/ios/DerivedData/
